---
title: "Branch Point — Harness Layout | harness.design"
sidebarTitle: "Branch Point"
description: "Add and position branch points in the layout view to join bundles. Supports splitting, merging, and locating inline splices, diodes, and resistors."
---

import { Steps } from 'nextra/components'
import { IconArrowsSplit2, IconEye, IconHash, IconCheck } from "@tabler/icons-react";


# Branch Point

A <IconArrowsSplit2/> `Branch Point` is used to join multiple bundles together.

Inline components that are located within the harness — such as [Splices](/component/splice), [Diodes](/component/diode), and [Resistors](/component/resistor) — can be positioned at branch points.

Branch Points can be multiple [Coverings](/part/covering) and [Boots](/part/connector/boot)

Branch points can only be added in the [Layout View](/quick-start/layout).

## Adding Branch Points

`right-click` on the canvas and select <IconArrowsSplit2/> `Branch Point`. This will add a branch point with no connected bundles. `left-click-drag` from the connection point of a connector, terminal, or another branch point to create a bundle to the new branch point. 

<br/>
<video src="/component/branch-point-add.mp4" width={634} style={{ borderRadius: '8px' }} autoPlay loop muted playsInline />

## Positioning Branch Points

`left-click-drag` to position a **Branch Point** on the canvas.  

## Merging Branch Points

To merge two branch points, `left-click-drag` one onto the other.

## Viewing Branch Point Component IDs

To view the component ID of a Branch Point on the **Schematic** and **Layout** views goto <IconEye/> `View Options` → <IconHash/> `Show component IDs` and <IconCheck/> enable it.