# Nets

Select any part or component to quickly visualize where it is used and what net it is connected to. 

## Selection of Wire
A wire selected in the any view will carry through to all the other views showing both the wire and the net it is connected to.
<br />
<img src="/features/nets-wire.png" width={800} style={{ borderRadius: '8px' }} />
