import { Callout } from 'nextra/components'
import { IconEye, IconBolt, IconManualGearbox, IconBrandStackshare, IconMinus } from "@tabler/icons-react";

# Nets

Select any part or component to quickly visualize where it is used across the views and the net it is connected to. The following are some examples:

<Callout>  
This feature can be disabled. To do this, on the **Schematic** and **Layout** views goto <IconEye/> `View Options` → <IconBolt/> `Show nets for selection` and <IconMinus/> **disable** it.
</Callout>

## Connector Pinout
Selecting a [Connector](/component/connector) will show all wires it is connected to and their path through the harness.  
<br />  
<img src="/features/nets-connector.png" width={800} style={{ borderRadius: '8px' }} />

## Looking Inside a Bundle
Selecting a [Bundle](/component/bundle) will highlight the wires going through it and show the nets they are connected to.  
<br />  
<img src="/features/nets-bundle.png" width={800} style={{ borderRadius: '8px' }} />

## Viewing a Wire's Net
A [Wire](/component/wire) selected in any view will carry through to all the other views, showing both the wire and the net it is connected to.  
<br />  
<img src="/features/nets-wire.png" width={800} style={{ borderRadius: '8px' }} />

## Part Usage
Selecting a [Part](/part) in the Parts screen will highlight where it is used in the other views.  
<br />  
<img src="/features/nets-part.png" width={800} style={{ borderRadius: '8px' }} />

## Net Labels
**Net Labels** can be used to label nets. See [Net Labels](/component/net-label) for more information.
<br />
<img src="/components/net-label-connections.png" width={800} style={{ borderRadius: '8px' }} />

## Highlight Wires Attached to Selected Part
There may be times when you do not want to see all the wires on the net. For example when you are selecting a connector you may only want to see the wires attached to that connector. To do this, on the **Schematic** and **Layout** views goto <IconEye/> `View Options` → <IconBolt/> `Show nets for selection` and <IconMinus/> disable it. Now when a part is selected only the wires attached to that part will be highlighted.
<br />
<img src="/features/nets-disable-result.png" width={800} style={{ borderRadius: '8px' }} />

