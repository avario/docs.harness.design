---
asIndexPage: true
title: "Parts View — BOM | harness.design"
sidebarTitle: "Parts View"
description: "The parts view lists every part in your wire harness with quantity, part number, and manufacturer. Add, edit, and import parts to build and export BOM."
---

import { 
  IconNut,
  IconPackage,
  IconPlus,
  IconColumns3,
  IconTable,
  IconArrowDown,
  IconArrowUp,
} from "@tabler/icons-react";
import { Callout } from "nextra/components";

# <IconNut/>Parts

The <IconNut/>**Parts** view is a visual representation of the [Part](/part) used in the harness. In this view you can add [Parts](/part), modify their properties and see the how many times they are used in the harness. This is used to generate the <IconTable/>**BOM**.

<br />
<img src="/view/parts/parts.png" width={800} style={{ borderRadius: "8px" }} />

## Table

This is the list of all your [Parts](/part) in the harness.

`right-click` anywhere on the canvas to add a [Part](/part) or click the <IconPlus/>**Add part** button.

`left-double-click` a parts value to edit it.

`left-double-click` a part to edit the part's properties. [More Info](/part#editing-parts)

Total Price is shown underneath the table. This is a sum of the individual totals in the table.

## Table Sorting

`left-click` the <IconArrowDown/> `Arrow` in the column header to sort the table by that column. `left-click` the arrow again to switch from <IconArrowDown/> descending to <IconArrowUp/>ascending sorting.

##  Buttons
<br/>
<ul
  style={{
    listStyleType: "none",
    paddingLeft: 0,
    display: "flex",
    flexDirection: "column",
    gap: "0.5rem"
  }}
>
  <li><IconPackage/>**Part Library** - Opens the [Parts library](/view/parts/library)</li>
  <li><IconPlus/>**Add part** - Adds a new part to the Parts view. [More Info](/part)</li>
  <li><IconColumns3/>**Columns** - Hide/ show part details in the table</li>
</ul>