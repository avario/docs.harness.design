import { Steps } from 'nextra/components'
import { Callout } from 'nextra/components'

# Export

Go to **Main Menu → Export** and select **Schematic**, **Layout**, **Wiring Table** or **BOM**.

<br />  
<img src="/quick-start/export-menu.png" width={381} style={{ borderRadius: '8px' }} />

<Callout>  
The **Schematic** and **Layout** options export as `.pdf` files.  This can be opened and modified in any vector graphics application that supports `.pdf`. 
</Callout>

<Callout>  
The **Wiring Table** and **BOM** options export as `.csv` files.  This can be opened and modified in any spreadsheet application. 
</Callout>